extend type Query {
    fetchEmailAuth(authId: ID): AuthenticationResult
    fetchGithubVerification(verificationId: ID): AuthenticationResult
    submitOAuthForAction(code: String!, provider: String!, loginClient: String): AuthenticationResult
}
extend type Mutation {
    submitEmailForAuth(email: String, loginClient: String!): EmailAuthAck
}
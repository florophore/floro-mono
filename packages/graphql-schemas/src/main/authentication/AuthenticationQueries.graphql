extend type Query {
    fetchEmailAuth(authId: ID): AuthenticationResult
}
extend type Mutation {
    submitOAuthForAction(code: String!, provider: String!): AuthenticationResult
    submitEmailForAuth(email: String): EmailAuthAck
}
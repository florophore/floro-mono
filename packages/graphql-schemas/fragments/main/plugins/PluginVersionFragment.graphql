fragment PluginVersion on PluginVersion {
  id
  name
  isPrivate
  version
  manifest
  codeDocsUrl
  codeRepoUrl
  description
  ownerType
  createdAt
  updatedAt
  pluginId
  managedCopy

  isBackwardsCompatible
  previousReleaseVersion
  state

  displayName
  lightIcon
  darkIcon
  selectedLightIcon
  selectedDarkIcon
  user {
    id
    username
  }
  organization {
    id
    handle
  }

  pluginDependencies {
    id
    name
    displayName
    description
    version
    isPrivate
    ownerType
    lightIcon
    darkIcon
    selectedLightIcon
    selectedDarkIcon
    pluginId
    user {
        id
        username
    }
    organization {
        id
        handle
    }
  }
}
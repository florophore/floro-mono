query searchPluginsForRepository(
  $query: String!
  $repositoryId: ID!
) {
  searchPluginsForRepository(
    query: $query
    repositoryId: $repositoryId
  ) {
    plugins {
      ...Plugin
    }
  }
}

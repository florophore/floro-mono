overwrite: true
schema: "./src/main/**/*.graphql"
documents: null
generates:
  src/generated/main-graphql.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      scalars:
        DateTime: string
        EmailAddress: string
        UUID: string
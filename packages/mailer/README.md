# Floro Mailer

We use mjml for rendering emails with React.

Run `yarn dev` to live test html emails on <a href="http://localhost:5173">localhost:5173</a> .

Examples of mock emails can be found in `./src/templates`;

## Mocking

You can create test cases by exporting variables suffixed with "mock" or "Mock".